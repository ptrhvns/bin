 #!/usr/bin/env bash

 set -e

if [ -e "spec/dummy" ]; then
    (
        cd spec/dummy
        bundle exec rake assets:clobber
        bundle exec rake log:clear
        bundle exec rake tmp:clear
    )
fi
